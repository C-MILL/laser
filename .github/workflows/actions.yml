name: Deploy to Raspberry Pi

on:
  push:
    branches:
      - production

jobs:
  deploy:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Verify Conda Environment
      run: |
        source /home/pi/miniforge3/etc/profile.d/conda.sh
        conda activate laser
        conda info --envs
        python -V
    - name: Setup SSH
      env:
        SSH_KEY: ${{ secrets.SSH_KEY }}
      run: |
        mkdir -p ~/.ssh
        echo "${SSH_KEY}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H raspberrypi >> ~/.ssh/known_hosts
    - name: Verify SSH Connection
        run: |
          ssh -i ~/.ssh/id_rsa pi@raspberrypi 'echo "SSH connection established"'

    - name: Deploy to Raspberry Pi
      run: |
        source /home/pi/miniforge3/etc/profile.d/conda.sh
        conda activate laser
        # Stop the existing service
        ssh -i ~/.ssh/id_rsa pi@raspberrypi 'sudo systemctl stop laser_app.service'
        # Copy new code
        rsync -avz -e "ssh -i ~/.ssh/id_rsa" . pi@raspberrypi:/home/pi/laser_app
        # Restart the service
        ssh -i ~/.ssh/id_rsa pi@raspberrypi 'sudo systemctl start laser_app.service'
      env:
        SSH_KEY: ${{ secrets.SSH_KEY }}
